---
'@weasel-js/labkit': patch
---

`<TrialOverview>` draws every annotation target's marks, read-only, where each target sits on the stage. `AnnotationsApi.paintedMarks(target)` answers a target's marks with the style its status and staleness resolve to — the same answer an export draws.
