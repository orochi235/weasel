---
'@weasel-js/ui': patch
---

An auto row's `AUTO` readout is no longer italic and draws at the extra-small size, a step below the row label. Its color reads `--wzl-secondary-fg`, falling back to `--wzl-fg-subtle` where a theme does not set one.
