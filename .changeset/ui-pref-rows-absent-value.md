---
'@weasel-js/ui': patch
---

A `SelectRow` whose value is absent or not one of its options now shows a "Choose option…" placeholder instead of the first option, and `CheckboxRow`, `TextRow` and `NumberRow` stay controlled when given no value.
