---
'@weasel-js/ui': patch
---

The property-panel family (`PropertyPanel`, `PropertyList`, rows, groups, subpanels and effect cards) is no longer text-selectable, so a drag that misses a slider does not paint a selection over the labels. Readouts, text fields and editable content stay selectable.
