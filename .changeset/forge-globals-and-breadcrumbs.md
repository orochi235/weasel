---
'@weasel-js/forge': patch
---

A global declared `toolbar: false` stays off the workshop toolbar and is set per trial only. Trial titles separate their segments with `>` rather than `/`, and leave out an `index` segment.
