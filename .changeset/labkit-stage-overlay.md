---
'@weasel-js/labkit': patch
---

A stage instrument can declare `stage.overlay`, a render function drawn over its content in viewport pixels, outside the camera. It is where a legend or a floating panel goes: anything that must sit on the picture without zooming with it.
