/**
 * `ModifierState` is declared in `@weasel-js/routing`, which is typed in it
 * throughout — the dispatcher, the invoker and the eligibility rules all read
 * one. Re-exported here so core's own call sites are unchanged.
 */
import type { ModifierState } from '@weasel-js/routing';
export type { ModifierState };
