{
  "comment": "Supplies compilerOptions for labkit's .d.ts emission (scripts/build-dts.mts → rollup-plugin-dts). Weasel specifiers resolve the ordinary way — workspace symlink, then the package's `exports` types condition — so this program spans labkit's own source only; the alias table in build-dts.mts is what redirects them to built declarations for bundling.",
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "jsx": "react-jsx",
    "strict": true,
    "exactOptionalPropertyTypes": false,
    "isolatedModules": true,
    "verbatimModuleSyntax": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "noEmit": true,
    "types": ["node"],
    "ignoreDeprecations": "6.0"
  },
  "include": ["src/**/*"]
}
